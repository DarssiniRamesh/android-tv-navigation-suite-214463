package org.example.app

import org.junit.Test
import org.junit.Assert.assertEquals

class DiscoveryTest {
    // PUBLIC_INTERFACE
    @Test
    /** Ensures JUnit4-based test discovery works on CI. */
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
